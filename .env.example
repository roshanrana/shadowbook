# Copy to .env. Never commit .env.
LEDGER_DB_USER=shadowbook
LEDGER_DB_PASSWORD=change-me
RECON_DB_USER=shadowbook
RECON_DB_PASSWORD=change-me
SHADOWBOOK_SEED=20260903
# Optional narrative drafting in reports (off by default; never used in tests)
SHADOWBOOK_NARRATIVE=0
# ANTHROPIC_API_KEY=   # only if SHADOWBOOK_NARRATIVE=1
