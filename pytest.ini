[pytest]
testpaths = legacy-sim/tests reconcile/tests report/tests
pythonpath = legacy-sim/src reconcile/src report/src
addopts = -q --strict-markers
markers =
    integration: needs a live PostgreSQL (SHADOWBOOK_TEST_DSN)
    scenario: a named adversarial scenario from the README
